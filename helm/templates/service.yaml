apiVersion: v1
kind: Service
metadata:
  name: {{ include "steam-markup-editor.fullname" . }}
  labels:
    {{- include "steam-markup-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "steam-markup-editor.selectorLabels" . | nindent 4 }}
