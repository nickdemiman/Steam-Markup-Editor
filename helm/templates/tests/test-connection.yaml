apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steam-markup-editor.fullname" . }}-test-connection"
  labels:
    {{- include "steam-markup-editor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "steam-markup-editor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
